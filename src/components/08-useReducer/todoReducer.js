/**
 *
 * @param {Array} state
 * @param {Object} action
 * @returns state
 */
export const todoReducer = (state = [], action) => {
  switch (action.type) {
    case 'add':
      break;

    default:
      return state;
  }
};
